{
  "name": "orangehrm-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --workers 1",
    "regression-test": "npx playwright test --grep @regression --workers 1",
    "smoke-test": "npx playwright test --grep @smoke",
    "test:headed": "npx playwright test --headed --workers 1",
    "test:debug": "npx playwright test --debug"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.5.2",
    "faker": "^6.6.6"
  }
}
